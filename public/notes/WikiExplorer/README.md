# WikiExplorer

**WikiExplorer** 是一个现代化的、基于文件系统的静态知识库生成器。它能够自动扫描你的本地 Markdown 笔记目录，生成美观、可搜索、支持大纲跳转的静态文档网站，并无缝部署到 GitHub Pages。

![Project Preview](./public/preview.png)

## ✨ 核心特性

-   **零迁移成本**: 直接读取现有的 Markdown 文件夹结构，无需修改文件元数据。
-   **智能索引**: 自动生成目录树，支持文件夹/文件排序。
-   **现代化阅读体验**:
    -   Typora 风格的阅读界面。
    -   支持 GFM (表格、任务列表)。
    -   **双侧边栏**: 左侧全局导航，右侧文件目录/文章大纲 (TOC)。
    -   代码高亮 (Atom One Dark)。
-   **隐私与过滤**: 支持通过 `.gitignore` 规则精确过滤敏感目录。
-   **自动化工作流**: 提供一键同步与部署脚本。

## 🚀 快速开始 (Fork & Deploy)

如果你想用 WikiExplorer 来展示你自己的笔记，请遵循以下步骤：

### 1. Fork 本项目
点击右上角的 **Fork** 按钮，将本项目复制到你的 GitHub 账号下。

### 2. 本地配置
将 Fork 后的仓库 Clone 到你的本地电脑，**建议将其放在你的笔记 Workspace 的根目录下**。

例如，如果你的笔记都在 `C:\MyNotes\` 下：
```bash
cd C:\MyNotes\
git clone https://github.com/YOUR_USERNAME/WikiExplorer.git
cd WikiExplorer
npm install
```

### 3. 配置个人信息
编辑 `WikiExplorer/author_profile/profile.json`，修改为你自己的信息：
```json
{
  "name": "Your Name",
  "bio": "Keep Learning",
  "email": "you@example.com",
  "github": "https://github.com/yourname",
  "avatar": "/profile-photo.jpg" 
}
```
*提示：你可以替换 `author_profile/profile-photo.jpg` 为你的头像图片。*

### 4. 运行与预览
在 `WikiExplorer` 目录下运行：
```bash
# 1. 扫描上级目录，同步笔记
npm run sync

# 2. 启动本地预览服务器
npm run dev
```
打开浏览器访问 `http://localhost:5173`，你应该能看到你的笔记已经被索引并展示出来了。

### 5. 自动化部署
本项目内置了 GitHub Pages 自动化部署流程。

1.  **提交代码**: 运行根目录下的 `do.bat` (Windows) 或手动执行：
    ```bash
    npm run sync
    git add .
git commit -m "Update notes"
git push
    ```
    *注意：必须先运行 sync，将笔记同步到 public 目录，Git 才能追踪到它们。*

2.  **开启 Pages**:
    -   进入 GitHub 仓库 **Settings** -> **Pages**。
    -   将 **Build and deployment** 源设置为 **GitHub Actions**。

3.  **等待构建**: 推送代码后，GitHub Actions 会自动构建并发布网站。

## ⚙️ 高级配置

### 忽略特定文件
项目会自动读取 Pages 工程根目录下的 `.gitignore` 文件。你可以添加规则来忽略不想公开的笔记目录：
```gitignore
# .gitignore
PrivateNotes/
Secrets/
*.key
```

### 自定义扫描逻辑
核心扫描脚本位于 `scripts/sync-notes.js`。默认配置会扫描 `WikiExplorer` 的**父级目录** (`../`)。如果你想修改扫描范围，可以编辑该文件中的路径配置。

## 🛠️ 技术栈
-   **Build**: Vite + Node.js Scripts
-   **Frontend**: React 18 + Tailwind CSS
-   **Markdown**: react-markdown + remark-gfm + rehype-slug
-   **Deploy**: GitHub Actions

## 📄 License
MIT License